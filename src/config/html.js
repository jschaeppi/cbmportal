const bonusRows = [];
const employeeNum = '';
const employeeName = '';
const comments = '';
const rowInfo = () => {

}

  const options = {
    format: 'letter',
    orientation: 'portrait',
  };

  module.export = {
      bonusRows: bonusRows,
      options: options
  }